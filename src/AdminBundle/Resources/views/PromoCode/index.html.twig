{% extends "@Admin/base.html.twig" %}

{% block title "Promocodes" %}

{% block body %}

    <table class="table card">
        <thead>
        <tr>
            <th width="75">id</th>
            <th width="250">promocode</th>
            <th>administrator</th>
            <th width="100">users</th>
            <th class="pull-right contains-button" width="100"><a href="{{ path('promo_code.create') }}" class="button button-small">create</a></th>
        </tr>
        </thead>
        <tbody>
        {% for promocode in promocodes %}
        <tr>
            <td>{{ promocode.id }}</td>
            <td>{{ promocode.code }}</td>
            <td><a href="{{ path('administrators.edit', { id: promocode.author.id }) }}">{{ promocode.author.username }}</a></td>
            <td>{{ promocode.users.count }}</td>
            <td class="pull-right">
                <a href="{{ path('promo_code.delete', { id: promocode.id }) }}">delete</a>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

    {% include '@Admin/pagination/pagination.html.twig' with { pagination: promocodes, path: 'promo_code.index.page' } only %}

{% endblock %}
